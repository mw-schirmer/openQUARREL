% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions_and_settings.R
\name{create_hydromad_model}
\alias{create_hydromad_model}
\title{Create a hydromad Model Object}
\usage{
create_hydromad_model(sma, cal_par, routing = "expuh")
}
\arguments{
\item{sma}{A string specifying the soil moisture accounting model (e.g., \code{"cmd"}, \code{"gr4j"}, \code{"sacramento"}).}

\item{cal_par}{A list of calibration parameters, typically created using \code{\link{set_hydromad_par}}.
Must contain \code{lower} and \code{upper} bounds for each parameter.}

\item{routing}{A string specifying the routing model (default is \code{"expuh"}).}
}
\value{
A \code{hydromad} model object with parameter ranges set.
}
\description{
Constructs a \code{hydromad} model using a specified soil moisture accounting (SMA) routine
and a set of default calibration parameters.
}
\details{
The function initializes a \code{hydromad} model with no data and updates it with the parameter
bounds provided in \code{cal_par}. This is useful for preparing a model structure before calibration.
}
\examples{
create_hydromad_model("cmd", default_cal_par[["cmd"]])

}
